# Profile A — skill + database only. Nothing runs in the background.
# Reviews are surfaced inside Claude via the /повтор command.
deployment_profile: skill-only
tts:
  provider: local
telegram:
  enabled: false
sync:
  mode: shared
