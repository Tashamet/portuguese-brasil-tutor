PyYAML>=6.0
requests>=2.31
# Default voice engine is Piper (piper-tts) — installed by install.sh, not listed
# here so a minimal `pip install -r requirements.txt` stays light. System TTS uses
# macOS `say`; cloud TTS uses ElevenLabs/OpenAI HTTP APIs via requests.
# ffmpeg/ffprobe must be on PATH for audio assembly. No aiogram/APScheduler —
# the notifier is cron-driven.
